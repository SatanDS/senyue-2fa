services:
  senyue-2fa:
    image: nginx:1.27-alpine
    container_name: senyue-2fa
    restart: unless-stopped
    ports:
      - "8070:80"
    volumes:
      - ./app:/usr/share/nginx/html:ro
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro

